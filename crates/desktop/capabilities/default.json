{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions de la fenêtre principale. Sans ce fichier, `event.listen` est refusé côté interface : la demande de fermeture émise par Rust n'est écoutée par personne, la fenêtre refuse de se fermer et il faut tuer le processus.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit"
  ]
}
